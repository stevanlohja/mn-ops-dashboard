import NetworkChangeView from "@/components/changes/NetworkChangeView";

export const metadata = {
  title: "Network Change — PO Dash 2.0",
};

export default function NetworkChangePage() {
  return <NetworkChangeView />;
}
